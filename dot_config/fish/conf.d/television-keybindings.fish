# Television custom keybindings

# Directory navigation with cd
# For iTerm2: Make sure Preferences > Profiles > Keys > Left Option Key is set to "Esc+"
bind \ec tv-cd-widget

# If Alt-C doesn't work, run `fish_key_reader` and press Alt-C to see the sequence
# Then replace \ec with whatever sequence it shows