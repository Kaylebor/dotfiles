# Documentation and meta files
README.md
AGENTS.md
CRUSH.md
chezmoi.toml
CLAUDE.md
PACKAGE_MANAGEMENT.md
SHELL_COMPLETIONS.md
ONEPASSWORD_SETUP.md
COMPANY_DOTFILES_PLAN.md
scripts/
docs/

# Repo-local helper scripts (invoked via sourceDir, not copied to $HOME)
scripts/homebrew/**
.dot_local/bin/homebrew_manager/**
.local/bin/chezmoi-sqlite
.local/bin/managed-configs-status
.local/bin/__pycache__/**
*.pyc
*.pyo

# Editor temporary files
flycheck_*.el
flycheck_*.tmpl

# Per-device configuration files (never tracked)
.config/emacs/sql-connections.el

# JSON configurations with device-specific fields (managed via merge script)
.cursor/settings.json
.cursor/mcp.json
.claude/settings.json
.config/zed/settings.json

# macOS-specific files to ignore on non-macOS systems
{{ if ne .chezmoi.os "darwin" -}}
# Emacs Plus configuration (macOS-only)
.config/emacs-plus/**

# Alternative Homebrew installation files (only ignore if not alternative install)
{{- if and (hasKey . "homebrewInstallType") (ne .homebrewInstallType "alternative") }}
.local/share/chezmoi-homebrew-state.db
{{- end }}
{{- end }}

# Linux-specific files to ignore on non-Linux systems
{{ if ne .chezmoi.os "linux" -}}
# Wayland/Hyprland specific
.config/hypr/**
.config/waybar/**
.local/bin/hypr-keybind-helper.py
.local/bin/idle-inhibit-status.sh
.local/bin/idle-inhibit-toggle.sh

# Linux notification daemon
.config/mako/**

# Qt theming (Linux desktop)
.config/qt6ct/**

# Arch Linux AUR helper
.config/paru/**

# Linux desktop entries
.local/share/applications/**

# Systemd user services
.config/systemd/**
{{- end }}

# Windows-specific files to ignore on non-Windows systems
{{ if ne .chezmoi.os "windows" -}}
# Currently no Windows-specific files
{{- end }}
